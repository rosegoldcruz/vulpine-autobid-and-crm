{
  "name": "vulpine-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "corepack pnpm --filter @vulpine/backoffice dev",
    "build": "corepack pnpm --filter @vulpine/backoffice build",
    "lint": "corepack pnpm --filter @vulpine/backoffice lint && corepack pnpm typecheck",
    "typecheck": "corepack pnpm --filter './packages/*' --if-present typecheck && corepack pnpm --filter @vulpine/backoffice typecheck",
    "test": "tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "tsx": "^4.20.6",
    "typescript": "5.7.3"
  }
}
