# Earlier Command Center backend prototype

This code arrived with the cloned Backoffice shell. It is retained for audit and is not the authoritative Vulpine Engine. The current Python/FastAPI engine is under `services/engine`.
